/**
 * Flag used to store browser/main boot up data
 */
export const K_BROWSER_STORAGE = "__browser-store";

/**
 * Flag used to check if our preload script is allowed to run
 */
export const K_PRELOAD_INIT_KEY = "allowed";

/**
 * Flag used to marshal a logger to the preload side of the house
 */
export const K_PRELOAD_LOGGER_KEY = "logger";

/**
 * Flag used to get the name of the window to capture
 */
export const K_CAPTURE_WIN = "capture-win";

/**
 * Flag used to store our browser signal configuration
 */
export const K_SIGNAL_CONFIG = "signal-conf";

/**
 * Flag used to store our browser application configuration
 */
export const K_BROWSER_CONFIG = "browser-conf";
